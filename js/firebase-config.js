/* ============================================================
   Word Solitaire — Firebase project config

   Fill this in with YOUR OWN Firebase project's values:
   Firebase console → ⚙ Project settings → General → "Your apps" →
   Web app (</>) → SDK setup and configuration → "Config".

   These values are meant to be public (they ship inside every visitor's
   browser) — Firebase access is controlled by the Firestore security
   rules you set in the console, not by keeping this file secret.

   Until you fill in a real apiKey below, cloud sync stays silently
   disabled and the game keeps working exactly as before, saving only
   to this browser's localStorage.
   ============================================================ */

const firebaseConfig = {
  apiKey: "AIzaSyBRQSCoLCuqhltNjP5gK_nob3NryViur3g",
  authDomain: "word-solitaire-0.firebaseapp.com",
  projectId: "word-solitaire-0",
  storageBucket: "word-solitaire-0.firebasestorage.app",
  messagingSenderId: "951753321497",
  appId: "1:951753321497:web:0aad4847b982d402b14e88",
};
